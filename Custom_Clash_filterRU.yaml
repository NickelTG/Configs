unified-delay: true        # Учитывает задержку рукопожатия, а не только пинг
tcp-concurrent: true       # Разрешает многопоточную обработку TCP
global-client-fingerprint: chrome # Маскировка под браузер
keep-alive-interval: 1800  # Держит соединения живыми дольше

proxy-groups:
  # Основная группа выбора
  - name: PROXY
    type: select
    proxies:
      - LOAD-BALANCE
      - LOAD-BALANCE_RR
      - auto
      - fallback
      - DIRECT

  # Мультиплексирование: распределение нагрузки
  - name: LOAD-BALANCE
    type: load-balance
    strategy: consistent-hashing
    use: &sub_list
      - AvenCores_sub_24
      - sub_hans_thomas
      - sub_v2go_de
      - sub_vless_universal
      - sub_proxify_mixed
    url: http://cp.cloudflare.com/generate_204
    interval: 300
  
  - name: LOAD-BALANCE_RR
    type: load-balance
    strategy: round-robin
    use: *sub_list
    url: http://cp.cloudflare.com/generate_204
    interval: 300

  # Авто-выбор самого быстрого сервера
  - name: auto
    type: url-test
    use: *sub_list
    url: http://cp.cloudflare.com/generate_204
    interval: 300
    tolerance: 50
    lazy: true

  - name: fallback
    type: fallback
    use: *sub_list
    url: http://cp.cloudflare.com/generate_204
    interval: 300

proxy-providers:
  # ОБЩИЙ БЛОК НАСТРОЕК (Якорь)
  common-settings: &common_filter
    type: http
    interval: 3600
    filter: "geoip: !ru"  # Фильтрация серверов (исключаем РФ)
    health-check: {enable: true, url: http://cp.cloudflare.com/generate_204, interval: 300}
    override: {udp: true}

  AvenCores_sub_24:
    <<: *common_filter
    url: "https://github.com/AvenCores/goida-vpn-configs/raw/refs/heads/main/githubmirror/24.txt"
    path: ./proxy_providers/sub_24.yml

  sub_hans_thomas:
    <<: *common_filter
    url: "https://raw.githubusercontent.com/hans-thomas/v2ray-subscription/master/servers.txt"
    path: ./proxy_providers/hans_thomas.yml

  sub_v2go_de:
    <<: *common_filter
    url: "https://raw.githubusercontent.com/Danialsamadi/v2go/main/Splitted-By-Country/DE.txt"
    path: ./proxy_providers/v2go_de.yml

  sub_vless_universal:
    <<: *common_filter
    url: "https://raw.githubusercontent.com/zieng2/wl/main/vless_universal.txt"
    path: ./proxy_providers/vless_universal.yml

  sub_proxify_mixed:
    <<: *common_filter
    url: "https://raw.githubusercontent.com/Firmfox/proxify/main/v2ray_configs/mixed/subscription-1.txt"
    path: ./proxy_providers/proxify_mixed.yml

rule-providers:
  skrepysh-proxy:
    type: http
    behavior: classical
    url: https://github.com/Skrepysh/mihomo-rulesets/raw/refs/heads/main/skrepysh-rulesets/skrepysh-proxy.yaml
    interval: 86400
  ru-bundle:
    type: http
    behavior: domain
    url: https://github.com/legiz-ru/mihomo-rule-sets/raw/main/ru-bundle/rule.yaml
    interval: 86400
  torrent-clients:
    type: http
    behavior: classical
    url: 'https://raw.githubusercontent.com/legiz-ru/mihomo-rule-sets/refs/heads/main/other/torrent-clients.yaml'
    interval: 86400
  torrent-trackers:
    type: http
    behavior: domain
    url: https://github.com/legiz-ru/mihomo-rule-sets/raw/main/other/torrent-trackers.mrs
    interval: 86400

rules:
  - RULE-SET,torrent-clients,DIRECT
  - RULE-SET,torrent-trackers,DIRECT
  - PROCESS-NAME,org.thoughtcrime.securesms,DIRECT
  - PROCESS-NAME,com.badoo.mobile,PROXY
  - PROCESS-NAME,com.bumble.app,PROXY
  - PROCESS-NAME,com.whatsapp,PROXY
  - PROCESS-NAME,ru.fourpda.client,PROXY
  - RULE-SET,skrepysh-proxy,PROXY
  - RULE-SET,ru-bundle,PROXY
  - MATCH,DIRECT
