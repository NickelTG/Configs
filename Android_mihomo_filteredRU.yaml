unified-delay: true
tcp-concurrent: true
global-client-fingerprint: chrome
keep-alive-interval: 1800

dns:
  enable: false  # ВАЖНО: вообще отключаем DNS Clash

proxy-groups:
  - name: VPN
    type: select
    proxies:
      - DIRECT

rules:
  - MATCH,DIRECT
